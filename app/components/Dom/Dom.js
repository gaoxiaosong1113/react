import React, { Component } from 'react';
import Button from '../Button/Button';
import Tab from '../Tab/Tab';
import Tabs from '../Tab/Tabs';

class Dom extends Component {
    render() {
        return <div > < Button / > < Tabs / > < /div>
    }
}


export default Dom;
